host: "0.0.0.0"
port: 7047

notarization:
  max_sent_data: 4096
  max_recv_data: 16384
  timeout: 120
  private_key_pem_path: "/app/notary.key"

tls:
  enabled: false  # Caddy reverse proxy handles HTTPS (Let's Encrypt → localhost:7047)

oracle:
  # No signing key configured — key is generated inside the TDX VM at each boot.
  # The private key never leaves TDX encrypted memory.
  contract_address: "0xbc2881e3f247F38a4B362bAF21829b17ea141Af1"  # JJSKIN on Arbitrum Sepolia
  chain_id: 421614  # Arbitrum Sepolia
  rpc_url: "https://sepolia-rollup.arbitrum.io/rpc"
  steam_factory_address: "0x627f747eb08442d0A61fC65093f21210142d013f"  # SteamAccountFactory on Arbitrum Sepolia
